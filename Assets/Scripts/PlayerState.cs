using UnityEngine;

//! TODO: This is where each player's state, as opposed to GameState[Client] should be managed.
// The view of other players cards would be managed here
// Playback events for this player would be stored here, although
// perhaps GameState[Client] could manage it for all players

public class PlayerState : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
